---
import SanityPicture from 'astro-sanity-picture';
type Props = {
  node: SanityTitledImage;
};
const { node } = Astro.props;
---

<SanityPicture
  img={{ sizes: '(min-width:768px) 60vw, 100vw', alt: node.alt }}
  src={node}
  class="mx-auto h-full w-full md:aspect-video"
/>
