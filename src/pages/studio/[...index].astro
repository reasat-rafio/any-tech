<div id="sanity-studio"></div>

<script>
  import { renderStudio, type WorkspaceOptions } from 'sanity';
  import config from '@/studio/sanity.config';

  renderStudio(
    document.getElementById('sanity-studio'),
    config as WorkspaceOptions
  );
</script>

<style>
  #sanity-studio {
    height: 100vh;
    max-height: 100dvh;
    overscroll-behavior: none;
    -webkit-font-smoothing: antialiased;
    overflow: auto;
  }
  *:not([data-ui='Popover__arrow']):not([data-ui='Tooltip__arrow']) > svg {
    display: inline;
  }
</style>
